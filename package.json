{
  "name": "atlas",
  "version": "1.5.6",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy --config wrangler.toml",
    "dev": "wrangler dev --config wrangler.toml",
    "check": "node --check atlaspanel.js && node --check atlaspanel-deployer.js"
  },
  "devDependencies": {
    "wrangler": "latest"
  }
}
